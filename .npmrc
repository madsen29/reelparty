# Expo / React Native + Metro need a flat node_modules layout to resolve
# peer deps (react, react-native) consistently across the monorepo.
node-linker=hoisted
shamefully-hoist=true
public-hoist-pattern[]=*

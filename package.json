{
  "name": "reelparty",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @reelparty/api\" \"npm run dev -w @reelparty/web\"",
    "dev:tunnel": "TUNNEL=1 concurrently -n api,web,tunnel -c blue,green,magenta \"npm run dev -w @reelparty/api\" \"npm run dev -w @reelparty/web\" \"ngrok http 127.0.0.1:5173\"",
    "dev:web": "npm run dev -w @reelparty/web",
    "dev:api": "npm run dev -w @reelparty/api",
    "dev:mobile": "npm run start -w @reelparty/mobile",
    "build": "npm run build -w @reelparty/web",
    "preview": "npm run preview -w @reelparty/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

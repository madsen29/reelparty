// Re-export the shared design-token preset so apps can do:
//   presets: [require("nativewind/preset"), require("@reelparty/ui/tailwind-preset")]
module.exports = require("@reelparty/config/tailwind");

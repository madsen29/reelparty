// MongoDB collections (created automatically on first use)
//
// parties:       { code, host_id, host_name, now_playing_id, created_at }
// members:       { id, party_code, name, color, joined_at }
// queue_items:   { id, party_code, url, platform, video_id, title, creator,
//                  thumbnail, added_by_id, added_by_name, watched_by, position, created_at }
